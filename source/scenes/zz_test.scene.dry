title: Test
subtitle: Just a test choice
order: 5
is-special: true
on-arrival: {!
if (!Q.holtext) {
    const qolEconomic = Q["qol-economic"] || "";
    const qolBalance = Q["qol-balance"] || "";
    const qolRelationships = Q["qol-relationships"] || "";
    const qolChallenge = Q["qol-challenge"] || "";
    const qolGrowth = Q["qol-growth"] || "";
    const qolPurpose = Q["qol-purpose"] || "";
    const qolAspiration = Q["qol-aspiration"] || "";
    const qolAccomplish = Q["qol-accomplish"] || "";
    const complement = Q.complement || "";
    const frbPeople = Q["frb-people"] || "";
    const frbEnvironment = Q["frb-environment"] || "";

    if (Q.player === "one") {
        Q.holtext = `I want ${qolEconomic} in a balanced life with ${qolBalance}. My relationships will be grounded in ${qolRelationships}. I thrive on ${qolChallenge} and want to be free to ${qolGrowth}. I find purpose in ${qolPurpose}. I seek to be ${qolAspiration} and want to contribute to ${qolAccomplish}.
${complement}

I will act in ways that are ${frbPeople}.

All to be ensured, for many generations to come, on a foundation of ${frbEnvironment}.`;
    } else if (Q.player === "group" || Q.player === "org") {
        Q.holtext = `We want ${qolEconomic} in balanced lives with ${qolBalance}. Our relationships will be grounded in ${qolRelationships}. We thrive on ${qolChallenge} and want to be free to ${qolGrowth}. We find purpose in ${qolPurpose}. We seek to be ${qolAspiration} and want to contribute to ${qolAccomplish}.
${complement}

We will act in ways that are ${frbPeople}.

All to be ensured, for many generations to come, on a foundation of ${frbEnvironment}.`;
    }
}
!}
new-page: true
tags: root

= Title


Paper: {!<h1>Holistic Context:</h1><br><div contenteditable="true" data-quality="holtext" style="white-space: pre-wrap;" oninput="window.dendryUI && window.dendryUI.dendryEngine && (window.dendryUI.dendryEngine.state.qualities[this.dataset.quality] = this.innerText);">!}[+ holtext +]{!</div>!}


{!<textarea class="textarea" data-quality="test" placeholder="Write your custom answer." oninput="window.dendryUI && window.dendryUI.dendryEngine && (window.dendryUI.dendryEngine.state.qualities[this.dataset.quality] = this.value);">!}[+ test +]{!</textarea>!}

Image: sailboat.jpg

<h2>Test heading</h2>

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('33_framework_outro'); return false;">Scene Outro</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('5_wum_intro');">5_wum_intro</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('13_qol_intro');">13_qol_intro</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('20_frb_people');">20_frb_people</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('22_holtext');">22_holtext</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('23_fq_intro');">23_fq_intro</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('31_fq_results');">31_fq_results</a>!}

In-text link: {!<a href="#" onclick="dendryUI.dendryEngine.goToScene('33_framework_outro');">33_framework_outro</a>!}

# TODO: hover-over

{!<a href="#" onmouseover="this.innerHTML='Mouse is over';" onmouseout="this.innerHTML='Mouseover';">Mouseover</a>!}

- @script_test

@script_test
title: I'd like to start the framework from the beginning
game-over: false

Me: I'd like to start the framework from the beginning.

Page will reload in a few seconds.

{!<meta http-equiv="refresh" content="20">!}

- @backSpecialScene: Back
